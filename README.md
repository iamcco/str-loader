# string-loader

string template loader for webpack

**install**

```
npm install --save-dev string-loader
```

**usage**

```
....
    loaders: [{
        test: /\.(S|s)tring$/,
        loader: 'string'
    }]
...

```
